@{
    Layout = "~/areas/bwum/views/shared/_indexlayout.cshtml";
}

@model IEnumerable<BaWuClub.Web.Dal.AdminType>
<div class="comm-btns">
    <h2>账号权限管理</h2>
    <div class="comm-btns-wrap">
        <a class="comm-btn0 iconfont icon-shuaxin" href="#" onclick="window.location.reload()"></a>
        <a class="comm-btn1 btn-general" href="#/level/create">创建</a>
        <a class="btn comm-btn2" href="#" data-url="/level/del">删除</a>
    </div>
</div>
<div id="notic-wrap">
    @Html.Raw(ViewBag.statusStr)
</div>
<form name="form" id="form" method="post">
    <table>
        <thead>
            <tr>
                <td width="40px;"><input type="checkbox" id="chk-all" onclick="cheakall(this)" /></td>
                <td width="40px;">Id</td>
                <td>权限名称</td>
                <td>发布时间</td>
                <td>更多操作</td>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (BaWuClub.Web.Dal.AdminType u in Model)
                {
                    <tr>
                        <td><input type="checkbox" name="chk" value="@u.Id" /></td>
                        <td>@u.Id</td>
                        <td>@u.Name</td>
                        <td>@u.VarDate</td>
                        <td><a href="#/level/edit/@u.Id">编辑</a></td>
                    </tr>
                }
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="10">
                    @Html.Raw(ViewBag.PageHtmlStr)
                </td>
            </tr>
        </tfoot>
    </table>
</form>